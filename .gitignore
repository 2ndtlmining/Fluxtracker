node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build
/.claude

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Data directory (runtime data, historical JSON — do not commit)
data/
src/data/

claude.md
# Supabase/R2 table backups pulled locally for analysis — never commit (tens of MB)
backups_*.json
